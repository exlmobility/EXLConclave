export var  CONFIG = {
    mock:  {
        // mock device as rooted
        isRooted : false
    },

    // Enable Disable Logs
    logging : true,
    // prevent run on rooted devices
    checkRoot : false,
   

}